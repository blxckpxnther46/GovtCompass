
export const RECOMMENDATION_WEIGHTS = {
  CATEGORY: 30,
  SUB_CATEGORY: 25,
  TAG: 10,
  BENEFICIARY: 15,
  STATE: 10,
};

export const MAX_SCORE =
  RECOMMENDATION_WEIGHTS.CATEGORY +
  RECOMMENDATION_WEIGHTS.SUB_CATEGORY +
  (RECOMMENDATION_WEIGHTS.TAG * 2) +
  RECOMMENDATION_WEIGHTS.BENEFICIARY +
  RECOMMENDATION_WEIGHTS.STATE;